# Go Worker Pool Implementation 🏗️

Этот проект демонстрирует паттерн **Worker Pool**. Вместо того чтобы запускать по одной горутине на каждую задачу, мы создаем фиксированное количество "воркеров", которые по очереди разбирают задачи из общего канала.
## 🤔 Какую проблему это решает:
Если у вас 100,000 задач, запуск 100,000 горутин может перегрузить систему. Пул воркеров позволяет ограничить количество одновременно работающих потоков (например, до 3), сохраняя при этом высокую производительность.



## 🛠 Как это работает в коде:

1. **`jobs := make(chan int)`**: Создается очередь задач.
2. **`go worker(...)`**: Запускается несколько воркеров в фоне. Каждый воркер — это отдельный "сотрудник".
3. **`sync.WaitGroup`**: Диспетчер (main) использует счетчик, чтобы дождаться, пока последний воркер "уйдет со смены" (`wg.Done()`).
4. **`close(jobs)`**: Когда диспетчер закрывает канал, все воркеры понимают, что новых задач не будет, и завершают работу.
